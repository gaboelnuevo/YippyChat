export const getUser = (state, id) => state.entities.users[id]

export const getChannel = (state, id) => state.entities.channels[id]

export const getMessage = (state, id) => state.entities.messages[id]
